{
  "name": "@ezu/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "vite",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm -w build && node --test \"dist/apps/web/src/index.test.js\""
  },
  "dependencies": {
    "@ezu/agent": "workspace:*",
    "@ezu/core": "workspace:*",
    "@ezu/protocol": "workspace:*",
    "@ezu/ui": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
